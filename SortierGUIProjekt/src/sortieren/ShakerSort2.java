package sortieren;

import guiProjekt.ArrayPanel;
import guiProjekt.SortierAusgabe;
import util.Util;

public class ShakerSort2 {
    /*
    int k;

    public int[] sort() {

        int i = 0, l = array4.length;
        while (i < l) {
            shaker1(i, l);
            l--;
            shaker2(i, l);
            i++;
        }
        return array4;
    }

    private void shaker1(int i, int l) {
        for (int j = i; j < l - 1; j++) {
            if (array4[j] > array4[j + 1]) {
                k = array4[j];
                array4[j] = array4[j + 1];
                array4[j + 1] = k;
            }
        }
    }

    private void shaker2(int i, int l) {
        for (int j = l - 1; j >= i; j--) {
            if (array4[j] > array4[j + 1]) {
                k = array4[j];
                array4[j] = array4[j + 1];
                array4[j + 1] = k;
            }
        }
    }

    public static void main(String[] args) {
        Shakersort shso = new Shakersort();
        int[] arr = shso.sort();
        for (int i = 0; i < arr.length; i++) {
            System.out.println(i + 1 + ": " + arr[i]);
        }
    }

}*/
}
